<% if main_object.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(main_object.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% main_object.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>